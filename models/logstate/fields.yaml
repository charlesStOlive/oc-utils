# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: waka.utils::lang.logstate.name
    slug:
        label: waka.utils::lang.global.slug
        preset: name
