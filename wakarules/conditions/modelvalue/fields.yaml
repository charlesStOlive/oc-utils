# ===================================
#  Field Definitions
# ===================================

fields:
    field:
        label: Nom du champs ou attribut | utiliser parent.child pour les valeurs des relations.
    operator:
        label: Opérateur
        type: dropdown
        options: listOperators
    value:
        label: Valeur
        comment: Si vous cherchez sur plusieurs valeurs separés les pat une , sans espaces. 
        trigger:
            field: operator
            action: hide
            condition: value[existe] || value[existepas]

